# Adlaire-Design 未タスク

本ファイルは、Adlaire-Designで未策定または未完了のまま残っているタスクだけを集約する管理ファイルである。

完了済みタスクと履歴は本ファイルに残さない。タスクが完了したら該当行を本ファイルから削除する。

---

## 1. 管理ルール

- 本ファイルには未完了タスクだけを記載する。
- タスクを完了した場合は、該当行を削除する。
- 完了済みタスクの保管場所として使わない。
- 未実装リストには、仕様確定済みで実装だけが未完了の項目だけを記載する。
- 仕様未確定、要否未決定、策定中の項目は未実装リストに含めない。
- タスクを追加、削除、分割、統合する場合は、必要に応じて `Docs/Master_Spec`、`Docs/Document_Index`、`Tools/check/check-adlaire-design.sh` を同期更新する。

---

## 2. 未タスク一覧

| ID | 分類 | 未タスク | 完了条件 | 主な更新対象 |
| --- | --- | --- | --- | --- |
| AD-TASK-024 | Icon Set | 公式アイコンセット拡張仕様策定。`AD-ICON-041` から `AD-ICON-240` までの追加アイコンを分類、用途、表示サイズ、種別、代替テキスト方針、実装状態まで決める。 | `Docs/Icon_Set_Catalog` に `AD-ICON-041` から `AD-ICON-240` までが `策定済み` として追加される。 | `Docs/Icon_Set_Catalog`、必要に応じて `Docs/Master_Spec`、`Docs/Document_Index`、`Tools/check/check-adlaire-design.sh` |
| AD-TASK-025 | Icon Set | 公式アイコンセット拡張SVG制作 第1弾。`AD-ICON-041` から `AD-ICON-080` までのSVG実体を制作する。 | `Icons/` に対応SVGが配置され、`Docs/Icon_Set_Catalog` の該当アイコンが `実装済み` になり、検査に通過する。 | `Icons/`、`Docs/Icon_Set_Catalog`、必要に応じて `Samples/design/`、`Tools/check/check-adlaire-design.sh` |
| AD-TASK-026 | Icon Set | 公式アイコンセット拡張SVG制作 第2弾。`AD-ICON-081` から `AD-ICON-120` までのSVG実体を制作する。 | `Icons/` に対応SVGが配置され、`Docs/Icon_Set_Catalog` の該当アイコンが `実装済み` になり、検査に通過する。 | `Icons/`、`Docs/Icon_Set_Catalog`、必要に応じて `Samples/design/`、`Tools/check/check-adlaire-design.sh` |
| AD-TASK-027 | Icon Set | 公式アイコンセット拡張SVG制作 第3弾。`AD-ICON-121` から `AD-ICON-160` までのSVG実体を制作する。 | `Icons/` に対応SVGが配置され、`Docs/Icon_Set_Catalog` の該当アイコンが `実装済み` になり、検査に通過する。 | `Icons/`、`Docs/Icon_Set_Catalog`、必要に応じて `Samples/design/`、`Tools/check/check-adlaire-design.sh` |
| AD-TASK-028 | Icon Set | 公式アイコンセット拡張SVG制作 第4弾。`AD-ICON-161` から `AD-ICON-200` までのSVG実体を制作する。 | `Icons/` に対応SVGが配置され、`Docs/Icon_Set_Catalog` の該当アイコンが `実装済み` になり、検査に通過する。 | `Icons/`、`Docs/Icon_Set_Catalog`、必要に応じて `Samples/design/`、`Tools/check/check-adlaire-design.sh` |
| AD-TASK-029 | Icon Set | 公式アイコンセット拡張SVG制作 第5弾。`AD-ICON-201` から `AD-ICON-240` までのSVG実体を制作する。 | `Icons/` に対応SVGが配置され、`Docs/Icon_Set_Catalog` の該当アイコンが `実装済み` になり、検査に通過する。 | `Icons/`、`Docs/Icon_Set_Catalog`、必要に応じて `Samples/design/`、`Tools/check/check-adlaire-design.sh` |

---

## 3. 未実装リスト

本章は、仕様確定済みで、実装だけが未完了の項目を管理する。

仕様未確定、要否未決定、策定中の項目は本章に含めない。それらは「未タスク一覧」で管理する。

| ID | 分類 | 未実装 | 完了条件 | 主な更新対象 |
| --- | --- | --- | --- | --- |
| 現時点で該当なし | - | - | - | - |
